# 配置文件（已废弃）
# 本应用现在使用命令行参数进行配置，不再使用环境变量
# 所有参数都有默认值，可以直接运行：cargo run
#
# 如需自定义配置，请使用以下命令行参数：
#
# cargo run -- \
#   --sqlite-database-url sqlite:auth.db \
#   --mysql-host localhost \
#   --mysql-port 3306 \
#   --mysql-username root \
#   --mysql-password your_password \
#   --mysql-database career_db \
#   --server-host 127.0.0.1 \
#   --server-port 8081 \
#   --jwt-secret your-secret-key
#
# 或者查看帮助信息：
# cargo run -- --help
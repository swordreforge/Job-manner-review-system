# 配置文件（已废弃）
# 本应用现在使用命令行参数进行配置，不再使用环境变量
# 请使用以下命令行参数启动应用：
#
# cargo run -- --sqlite-database-url sqlite:auth.db --mysql-database-url mysql://root:password@localhost/career_db --server-host 127.0.0.1 --server-port 8081 --jwt-secret your-secret-key
#
# 或者查看帮助信息：
# cargo run -- --help
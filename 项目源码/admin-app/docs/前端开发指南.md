# 教师端前端开发指南

## 概述

本文档介绍如何为教师端 API 开发前端界面，并利用 Rust 的嵌入式静态文件功能将前端资源打包到二进制文件中。

## 技术架构

### 后端特性
- **框架**: Rust + Actix-web
- **静态文件嵌入**: 使用 `rust-embed` 在编译时将静态文件嵌入二进制
- **API 基础路径**: `http://127.0.0.1:8081`
- **嵌入式路径**: `/` → 返回嵌入的 `index.html`

### 前端技术栈
- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **UI 组件库**: Element Plus
- **HTTP 客户端**: Axios
- **状态管理**: Pinia
- **路由**: Vue Router
- **图表库**: ECharts

## 开发流程

### 1. 初始化前端项目

```bash
cd template

# 创建 Vue 3 项目
npm create vue@latest teacher-frontend

# 进入项目目录
cd teacher-frontend

# 安装依赖
npm install element-plus axios pinia vue-router echarts

# 启动开发服务器
npm run dev
```

### 2. 项目结构

```
template/
├── teacher-frontend/       # 前端源码目录
│   ├── public/
│   ├── src/
│   │   ├── api/           # API 接口封装
│   │   ├── assets/        # 静态资源
│   │   ├── components/    # 公共组件
│   │   ├── router/        # 路由配置
│   │   ├── stores/        # 状态管理
│   │   ├── utils/         # 工具函数
│   │   ├── views/         # 页面组件
│   │   ├── App.vue
│   │   └── main.js
│   ├── package.json
│   └── vite.config.js
├── index.html              # 嵌入式入口文件（占位）
└── dist/                   # 构建输出目录
```

### 3. 配置 Vite

```javascript
// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  base: '/', // 静态资源基础路径
  build: {
    outDir: '../dist', // 输出到 template/dist
    emptyOutDir: true,
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8081',
        changeOrigin: true
      }
    }
  }
})
```

### 4. API 配置

```javascript
// src/utils/request.js
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 10000
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  response => {
    return response.data
  },
  error => {
    ElMessage.error(error.message || '请求失败')
    return Promise.reject(error)
  }
)

export default request
```

### 5. 开发工作流

#### 开发模式
```bash
# 终端 1: 启动后端服务
cd teacher-app
cargo run

# 终端 2: 启动前端开发服务器
cd template/teacher-frontend
npm run dev
```

访问 `http://localhost:5173` 开发前端，API 请求会自动代理到后端。

#### 构建生产版本
```bash
# 构建前端
cd template/teacher-frontend
npm run build

# 构建产物输出到 template/dist/

# 删除源码目录（可选，只保留构建产物）
cd ..
rm -rf teacher-frontend
```

#### 编译 Rust 二进制文件
```bash
cd teacher-app
cargo build --release

# 前端静态文件已嵌入到二进制中
./target/release/teacher-api
```

### 6. 功能模块开发

#### 6.1 登录页面

```vue
<!-- src/views/Login.vue -->
<template>
  <div class="login-container">
    <el-card class="login-card">
      <h2>教师端登录</h2>
      <el-form :model="loginForm" :rules="rules" ref="loginFormRef">
        <el-form-item prop="username">
          <el-input 
            v-model="loginForm.username" 
            placeholder="用户名"
            size="large"
          />
        </el-form-item>
        <el-form-item prop="password">
          <el-input 
            v-model="loginForm.password" 
            type="password"
            placeholder="密码"
            size="large"
          />
        </el-form-item>
        <el-form-item>
          <el-button 
            type="primary" 
            @click="handleLogin"
            size="large"
            style="width: 100%"
          >
            登录
          </el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { login } from '@/api/auth'

const router = useRouter()
const loginFormRef = ref()
const loginForm = ref({
  username: '',
  password: ''
})

const rules = {
  username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
}

const handleLogin = async () => {
  await loginFormRef.value.validate()
  const res = await login(loginForm.value)
  localStorage.setItem('token', res.data.token)
  router.push('/')
}
</script>

<style scoped>
.login-container {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: #1a1a1a;
}

.login-card {
  width: 400px;
  padding: 20px;
}

h2 {
  text-align: center;
  color: #fff;
  margin-bottom: 30px;
}
</style>
```

#### 6.2 学生列表页面

```vue
<!-- src/views/Student/List.vue -->
<template>
  <div class="student-list">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>学生列表</span>
          <el-button type="primary" @click="handleAdd">添加学生</el-button>
        </div>
      </template>

      <!-- 搜索栏 -->
      <el-form :inline="true" :model="searchForm">
        <el-form-item>
          <el-input 
            v-model="searchForm.keyword" 
            placeholder="搜索姓名或学号"
            @change="fetchStudents"
          />
        </el-form-item>
      </el-form>

      <!-- 数据表格 -->
      <el-table :data="students" style="width: 100%" stripe>
        <el-table-column prop="name" label="姓名" />
        <el-table-column prop="student_id" label="学号" />
        <el-table-column prop="class_name" label="班级" />
        <el-table-column label="操作" width="200">
          <template #default="{ row }">
            <el-button size="small" @click="handleView(row)">查看</el-button>
            <el-button size="small" @click="handleEdit(row)">编辑</el-button>
            <el-button size="small" type="danger" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>

      <!-- 分页 -->
      <el-pagination
        v-model:current-page="pagination.page"
        v-model:page-size="pagination.pageSize"
        :total="pagination.total"
        @current-change="fetchStudents"
        layout="total, prev, pager, next"
      />
    </el-card>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { getStudents, deleteStudent } from '@/api/student'
import { ElMessage, ElMessageBox } from 'element-plus'

const router = useRouter()
const students = ref([])
const searchForm = ref({ keyword: '' })
const pagination = ref({ page: 1, pageSize: 20, total: 0 })

const fetchStudents = async () => {
  const res = await getStudents({
    page: pagination.value.page,
    page_size: pagination.value.pageSize,
    keyword: searchForm.value.keyword
  })
  students.value = res.data.items
  pagination.value.total = res.data.total
}

const handleView = (row) => {
  router.push(`/students/${row.id}`)
}

const handleEdit = (row) => {
  router.push(`/students/${row.id}/edit`)
}

const handleDelete = async (row) => {
  try {
    await ElMessageBox.confirm('确定删除该学生吗？', '提示', {
      type: 'warning'
    })
    await deleteStudent(row.id)
    ElMessage.success('删除成功')
    fetchStudents()
  } catch (error) {
    // 用户取消
  }
}

onMounted(() => {
  fetchStudents()
})
</script>

<style scoped>
.student-list {
  padding: 20px;
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.el-pagination {
  margin-top: 20px;
  justify-content: center;
}
</style>
```

### 7. 路由配置

```javascript
// src/router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/stores/user'

const routes = [
  {
    path: '/login',
    component: () => import('@/views/Login.vue'),
    meta: { requiresAuth: false }
  },
  {
    path: '/',
    component: () => import('@/views/Layout.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: '',
        redirect: '/students'
      },
      {
        path: 'students',
        component: () => import('@/views/Student/List.vue')
      },
      {
        path: 'students/:id',
        component: () => import('@/views/Student/Detail.vue')
      },
      {
        path: 'tests',
        component: () => import('@/views/Test/List.vue')
      },
      {
        path: 'ops/status',
        component: () => import('@/views/Ops/Status.vue')
      },
      {
        path: 'ops/backup',
        component: () => import('@/views/Ops/Backup.vue')
      }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  
  if (to.meta.requiresAuth && !userStore.token) {
    next('/login')
  } else if (to.path === '/login' && userStore.token) {
    next('/')
  } else {
    next()
  }
})

export default router
```

### 8. 大屏适配

#### CSS 缩放方案

```css
/* src/assets/styles/common.css */
:root {
  --base-font-size: 16px;
}

html {
  font-size: var(--base-font-size);
}

/* 大屏适配 */
@media (min-width: 2560px) {
  :root {
    --base-font-size: 20px;
  }
}

@media (min-width: 3840px) {
  :root {
    --base-font-size: 24px;
  }
}

/* 使用 rem 单位 */
body {
  font-size: 1rem;
}

h1 { font-size: 2.25rem; }
h2 { font-size: 1.875rem; }
h3 { font-size: 1.5rem; }
```

#### 深色主题配置

```javascript
// src/assets/styles/theme.js
export const darkTheme = {
  '--el-bg-color': '#1a1a1a',
  '--el-bg-color-overlay': '#2a2a2a',
  '--el-text-color-primary': '#ffffff',
  '--el-text-color-regular': '#e0e0e0',
  '--el-border-color': '#404040',
  '--el-border-color-light': '#505050',
  '--el-color-primary': '#409eff',
  '--el-color-success': '#67c23a',
  '--el-color-warning': '#e6a23c',
  '--el-color-danger': '#f56c6c'
}
```

### 9. 构建和部署

#### 完整构建流程

```bash
#!/bin/bash
# build.sh

echo "构建前端..."
cd template/teacher-frontend
npm run build

echo "清理源码..."
cd ..
rm -rf teacher-frontend

echo "构建后端..."
cd ../teacher-app
cargo build --release

echo "构建完成！"
echo "可执行文件: ./target/release/teacher-api"
```

#### 一键部署脚本

```bash
#!/bin/bash
# deploy.sh

# 拉取最新代码
git pull

# 构建前端
cd template/teacher-frontend
npm install
npm run build

# 清理源码
cd ..
rm -rf teacher-frontend

# 构建后端
cd ../teacher-app
cargo build --release

# 停止旧服务
systemctl stop teacher-api

# 复制新版本
cp target/release/teacher-api /usr/local/bin/

# 启动服务
systemctl start teacher-api

echo "部署完成！"
```

### 10. 开发环境配置

#### 环境变量

```bash
# .env.development
VITE_API_BASE_URL=http://127.0.0.1:8081/api
```

```bash
# .env.production
VITE_API_BASE_URL=/api
```

#### Systemd 服务配置

```ini
# /etc/systemd/system/teacher-api.service
[Unit]
Description=Teacher API Service
After=network.target

[Service]
Type=simple
User=teacher
WorkingDirectory=/opt/teacher-app
ExecStart=/usr/local/bin/teacher-api
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

## 常见问题

### Q1: 如何在开发时调试嵌入式静态文件？

开发时使用 Vite 的开发服务器，构建后再测试嵌入式版本：

```bash
# 开发阶段
npm run dev

# 测试嵌入式版本
npm run build
cd ../teacher-app
cargo run
```

### Q2: 如何更新前端静态文件？

```bash
# 重新构建前端
cd template/teacher-frontend
npm run build

# 重新编译 Rust
cd ../teacher-app
cargo build --release
```

### Q3: 如何处理静态资源缓存？

在 `vite.config.js` 中配置文件名哈希：

```javascript
build: {
  rollupOptions: {
    output: {
      chunkFileNames: 'assets/js/[name]-[hash].js',
      entryFileNames: 'assets/js/[name]-[hash].js',
      assetFileNames: 'assets/[ext]/[name]-[hash].[ext]'
    }
  }
}
```

### Q4: 如何优化大屏加载速度？

1. 使用 CDN 加速静态资源
2. 启用 Gzip 压缩
3. 懒加载路由组件
4. 图片使用 WebP 格式
5. 减少不必要的依赖

## 附录

### A. API 接口文档

详见 `api/teacher.api` 文件

### B. 项目依赖

```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0",
    "element-plus": "^2.5.0",
    "axios": "^1.6.0",
    "echarts": "^5.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

### C. 推荐的开发工具

- **IDE**: VS Code + Volar
- **浏览器**: Chrome DevTools
- **API 测试**: Postman / Insomnia
- **版本控制**: Git

## 更新日志

- 2026-04-11: 初始版本
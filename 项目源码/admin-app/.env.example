# 数据库配置（可选）
# 如果不设置 DATABASE_URL，启动时会交互式询问数据库配置信息
# 格式: mysql://用户名:密码@主机:端口/数据库名
DATABASE_URL=mysql://root:your_password@localhost/career_db

# 服务器配置（可选，默认值：127.0.0.1:8081）
SERVER_HOST=127.0.0.1
SERVER_PORT=8081

# JWT密钥（可选，生产环境请修改）
JWT_SECRET=your-secret-key-change-in-production